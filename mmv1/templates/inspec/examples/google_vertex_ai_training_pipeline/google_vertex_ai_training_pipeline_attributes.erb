gcp_project_id = input(:gcp_project_id, value: '<%= external_attribute(pwd, 'gcp_project_id') -%>', description: 'The GCP project identifier.')

  training_pipeline = input('training_pipeline', value: <%= JSON.pretty_generate(grab_attributes(pwd)['training_pipeline']) -%>, description: 'training_pipeline description')