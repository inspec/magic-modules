<% gcp_project_id = "#{external_attribute(pwd, 'gcp_project_id', doc_generation)}" -%>
  <% batch = grab_attributes(pwd)['batch'] -%>
  describe google_dataproc_batches(parent: <%= doc_generation ? "' #{batch['parent']}'":"batch['parent']" -%>) do
    it { should exist }
  end