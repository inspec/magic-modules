<% gcp_project_id = "#{external_attribute(pwd, 'gcp_project_id', doc_generation)}" -%>
  <% organization_environment = grab_attributes(pwd)['organization_environment'] -%>
  describe google_apigee_organization_environments(parent: <%= doc_generation ? "' #{organization_environment['parent']}'":"organization_environment['parent']" -%>) do
    it { should exist }
  end