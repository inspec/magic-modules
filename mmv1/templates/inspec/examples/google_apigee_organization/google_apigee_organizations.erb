<% gcp_project_id = "#{external_attribute(pwd, 'gcp_project_id', doc_generation)}" -%>
  <% organization = grab_attributes(pwd)['organization'] -%>
  describe google_apigee_organizations(parent: <%= doc_generation ? "' #{organization['parent']}'":"organization['parent']" -%>) do
    it { should exist }
  end