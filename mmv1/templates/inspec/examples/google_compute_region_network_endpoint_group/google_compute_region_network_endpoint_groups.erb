<% gcp_project_id = "#{external_attribute(pwd, 'gcp_project_id', doc_generation)}" -%>
  <% region_network_endpoint_group = grab_attributes(pwd)['region_network_endpoint_group'] -%>
  describe google_compute_region_network_endpoint_groups(project: <%= gcp_project_id -%>, region: <%= doc_generation ? "' #{region_network_endpoint_group['region']}'":"region_network_endpoint_group['region']" -%>) do
    it { should exist }
  end