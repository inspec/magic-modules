<% gcp_project_id = "#{external_attribute(pwd, 'gcp_project_id', doc_generation)}" -%>
  <% custom_job = grab_attributes(pwd)['custom_job'] -%>
  describe google_vertex_ai_custom_job(parent: <%= doc_generation ? "' #{custom_job['parent']}'":"custom_job['parent']" -%>) do
    it { should exist }
  end