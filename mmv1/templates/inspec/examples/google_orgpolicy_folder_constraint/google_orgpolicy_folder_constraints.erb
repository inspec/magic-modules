<% gcp_project_id = "#{external_attribute(pwd, 'gcp_project_id', doc_generation)}" -%>
  <% folder_constraint = grab_attributes(pwd)['folder_constraint'] -%>
  describe google_orgpolicy_folder_constraints(parent: <%= doc_generation ? "' #{folder_constraint['parent']}'":"folder_constraint['parent']" -%>) do
    it { should exist }
  end