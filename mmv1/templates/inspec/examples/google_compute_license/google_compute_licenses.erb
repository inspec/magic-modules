<% gcp_project_id = "#{external_attribute(pwd, 'gcp_project_id', doc_generation)}" -%>
  <% license = grab_attributes(pwd)['license'] -%>
  describe google_compute_v1_licenses(project: <%= gcp_project_id -%>) do
    it { should exist }
  end