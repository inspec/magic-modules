gcp_project_id = input(:gcp_project_id, value: '<%= external_attribute(pwd, 'gcp_project_id') -%>', description: 'The GCP project identifier.')

  vpn_gateway = input('vpn_gateway', value: <%= JSON.pretty_generate(grab_attributes(pwd)['vpn_gateway']) -%>, description: 'vpn_gateway description')