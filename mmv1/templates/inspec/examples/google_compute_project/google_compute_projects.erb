<% gcp_project_id = "#{external_attribute(pwd, 'gcp_project_id', doc_generation)}" -%>
  <% project = grab_attributes(pwd)['project'] -%>
  describe google_compute_projects(project: <%= gcp_project_id -%>, region: <%= doc_generation ? "' #{project['region']}'":"project['region']" -%>) do
    it { should exist }
  end