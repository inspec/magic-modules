<% gcp_project_id = "#{external_attribute(pwd, 'gcp_project_id', doc_generation)}" -%>
  <% service = grab_attributes(pwd)['service'] -%>
  describe google_run_services(parent: <%= doc_generation ? "' #{service['parent']}'":"service['parent']" -%>) do
    it { should exist }
  end