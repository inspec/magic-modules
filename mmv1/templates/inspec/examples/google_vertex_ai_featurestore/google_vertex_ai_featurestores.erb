<% gcp_project_id = "#{external_attribute(pwd, 'gcp_project_id', doc_generation)}" -%>
  <% featurestore = grab_attributes(pwd)['featurestore'] -%>
  describe google_vertex_ai_featurestores(parent: "projects/#{gcp_project_id}/locations/#{featurestore['region']}", region: <%= doc_generation ? "' #{featurestore['region']}'":"featurestore['region']" -%>) do
    it { should exist }
  end