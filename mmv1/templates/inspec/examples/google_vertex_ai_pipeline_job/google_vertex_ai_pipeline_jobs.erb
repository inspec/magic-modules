<% gcp_project_id = "#{external_attribute(pwd, 'gcp_project_id', doc_generation)}" -%>
  <% pipeline_job = grab_attributes(pwd)['pipeline_job'] -%>
  describe google_vertex_ai_pipeline_jobs(parent: "projects/#{gcp_project_id}/locations/#{pipeline_job['region']}", region: <%= doc_generation ? "' #{pipeline_job['region']}'":"pipeline_job['region']" -%>) do
    it { should exist }
  end