<% gcp_project_id = "#{external_attribute(pwd, 'gcp_project_id', doc_generation)}" -%>
  <% region_url_map = grab_attributes(pwd)['region_url_map'] -%>
  describe google_compute_region_url_maps(project: <%= gcp_project_id -%>, region: <%= doc_generation ? "' #{region_url_map['region']}'":"region_url_map['region']" -%>) do
    it { should exist }
  end