gcp_project_id = input(:gcp_project_id, value: '<%= external_attribute(pwd, 'gcp_project_id') -%>', description: 'The GCP project identifier.')

  region_url_map = input('region_url_map', value: <%= JSON.pretty_generate(grab_attributes(pwd)['region_url_map']) -%>, description: 'region_url_map description')