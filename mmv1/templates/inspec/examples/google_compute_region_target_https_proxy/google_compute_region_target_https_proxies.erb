<% gcp_project_id = "#{external_attribute(pwd, 'gcp_project_id', doc_generation)}" -%>
  <% region_target_https_proxy = grab_attributes(pwd)['region_target_https_proxy'] -%>
  describe google_compute_region_target_https_proxies(project: <%= gcp_project_id -%>, region: <%= doc_generation ? "' #{region_target_https_proxy['region']}'":"region_target_https_proxy['region']" -%>) do
    it { should exist }
  end