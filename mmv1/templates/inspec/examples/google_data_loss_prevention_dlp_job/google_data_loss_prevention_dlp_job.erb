<% gcp_project_id = "#{external_attribute(pwd, 'gcp_project_id', doc_generation)}" -%>
<% dlp = grab_attributes(pwd)['dlp'] -%>
describe google_data_loss_prevention_dlp_job(project: <%= gcp_project_id -%>, name: <%= doc_generation ? "'#{dlp['name']}'" : "dlp['name']" -%>) do
  it { should exist }
end

describe google_data_loss_prevention_dlp_job(project: <%= gcp_project_id -%>, name: 'nonexistent') do
  it { should_not exist }
end