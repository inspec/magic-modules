<% gcp_project_id = "#{external_attribute(pwd, 'gcp_project_id', doc_generation)}" -%>
  <% nas_job = grab_attributes(pwd)['nas_job'] -%>
  describe google_vertex_ai_nas_jobs(parent: "projects/#{gcp_project_id}/locations/#{nas_job['region']}", region: <%= doc_generation ? "' #{nas_job['region']}'":"nas_job['region']" -%>) do
    it { should exist }
  end