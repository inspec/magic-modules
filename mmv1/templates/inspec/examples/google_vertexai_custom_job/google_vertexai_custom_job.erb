<% gcp_project_id = "#{external_attribute(pwd, 'gcp_project_id', doc_generation)}" -%>
   <% custom_job = grab_attributes(pwd)['custom_job'] -%>
   describe google_vertexai_custom_job(name: <%= doc_generation ? "' #{custom_job['name']}'":"custom_job['name']" -%>) do
     it { should exist }
   end