<% gcp_project_id = "#{external_attribute(pwd, 'gcp_project_id', doc_generation)}" -%>
  <% schedule = grab_attributes(pwd)['schedule'] -%>
  describe google_vertex_ai_schedules(parent: "projects/#{gcp_project_id}/locations/#{schedule['region']}", region: <%= doc_generation ? "' #{schedule['region']}'":"schedule['region']" -%>) do
    it { should exist }
  end