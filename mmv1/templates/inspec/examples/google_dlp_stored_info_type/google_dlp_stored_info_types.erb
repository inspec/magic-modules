<% gcp_project_id = "#{external_attribute(pwd, 'gcp_project_id', doc_generation)}" -%>
<% dlp = grab_attributes(pwd)['dlp'] -%>


describe google_dlp_stored_info_types(parent: "projects/#{<%= gcp_project_id -%>}/locations/#{<%= doc_generation ? "'#{dlp['location']}'" : "dlp['location']" -%>}") do
it { should exist }
its('names') { should include <%= doc_generation ? "'#{dlp['name']}'" : "dlp['name']" -%> }
end
